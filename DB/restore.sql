-- =====================================================================
--  restore.sql : 전체 복원 진입점 (스키마 → 테이블 → 데이터 순서)
--
--  사용법 (scrap_sprout_db 폴더 안에서 실행):
--    mysql -uroot -p < restore.sql
--
--  ※ SOURCE는 현재 작업 디렉터리 기준 상대경로로 파일을 찾습니다.
--    반드시 이 파일이 있는 폴더에서 실행하세요.
-- =====================================================================

SOURCE 01_schema.sql;
SOURCE 02_tables.sql;
SOURCE 03_data.sql;
